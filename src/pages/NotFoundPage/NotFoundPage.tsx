import { Component } from 'react';
import { NotFoundPageTemplate } from './NotFoundPage.template';

export default class NotFoundPage extends Component {
  render() {
    return NotFoundPageTemplate();
  }
}